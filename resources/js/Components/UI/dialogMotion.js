/**
 * Parâmetros partilhados: login, diálogos e offcanvas (ver `Login.jsx` + `AnimatedContent`).
 */
export const DIALOG_MOTION = {
    playOnMount: true,
    distance: 72,
    duration: 2.8,
    ease: 'power3.out',
    threshold: 0.1,
};
